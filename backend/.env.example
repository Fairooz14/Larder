PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
CORS_ORIGIN=http://localhost:5173

# Optional. Leave blank (or set to "demo") to run entirely on the built-in
# mock recipe dataset, no signup required. Get a free key at
# https://spoonacular.com/food-api/console#Dashboard (150 requests/day free)
# and paste it here to switch to real, live recipe search.
SPOONACULAR_API_KEY=
